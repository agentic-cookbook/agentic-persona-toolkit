{
  "name": "@agenticdevelopertoolkit/registry-types",
  "version": "0.1.0",
  "description": "Framework-free registry field-type catalog: type ids, config shapes, validators and value coercion. No React, no runtime dependencies — the backend vendors this file so both sides validate owner-defined values against the same rules.",
  "license": "UNLICENSED",
  "type": "module",
  "files": ["dist"],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup && pnpm run build:types",
    "build:types": "tsc --emitDeclarationOnly -p tsconfig.build.json",
    "dev": "tsup --watch",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^4.1.11"
  }
}
